// let oldArray = ['Shohruh', 'Baxodir', 'Mansur', 'Malohat'];

// let newArray = oldArray.map(name => {
//     return `${name}`
// })

// console.log(newArray)




// Umir aka

let Menu = {
    hotdog: 3000,
    hamburger: 5000,
    lavash: 12000
}

const Plus = (n) =>{
   soni = 4;
   console.log(soni*Object.values(Menu)[n]) 
}

Plus(0);
